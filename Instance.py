"""
    insinstance is return True  if the specified object is of the specified type, otherwise False
    isinstance(object, type)
    type --> A type or a class, or a tuple of types and/or classes


    --> Type เราต้องระบุประเภท Type ประเภทหนึ่งเช่น int ,double เป็นต้น
    --> Object จะต้องเป็นประเภทเดียวกับ Type 
    ---> instance จะเป็น True ก็ต่อเมื่อ Type และ Object เป็นประเภทเดียวกัน ถ้าไม่จะเป็น Flase
"""

x = isinstance(5, int)

print(x)
